# KotlinLogin
Kotlin login script.
Retrofit. 
On the server side is a PHP.

Open ApiLogin.kt under the rest folder and change var BASE_URL:String="URL TO YOUR SERVER"
In the root of application find folder "PHPBackend". 
Open Authentication.php and change your server parameters.
Open createtable.sql and copy data to your server database.

